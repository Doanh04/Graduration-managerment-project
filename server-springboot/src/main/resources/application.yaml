server:
  port: 8080
  servlet:
    context-path: /graduration
spring:
  application:
    name: graduration-managerment
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  datasource:
    url: "jdbc:mysql://localhost:3306/graduration-managerment"
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password: doanh@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    mongodb:
      uri: mongodb://localhost:27017/graduration_managerment

jwt:
  signerKey: "tgWNKP/9+GK8SlEph/A1WRAHz2KLXhw1ccWCwD1yphs="
  valid-duration: 3600
  refreshable-duration: 36000

app:
  cookie:
    secure: false
  storage:
    root: ${APP_STORAGE_ROOT:./storage}
